# Unreleased application changes

Store application changelog records that are not published yet. Use `fcvw/changelog@1`, link completed plans, record observed validation, and move or update the record through the release workflow. FCVW framework releases belong in `FCVW/framework-releases/`.
