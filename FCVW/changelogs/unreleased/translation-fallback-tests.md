## Added tests for translation fallback

- Added `tests/translation.test.js` to explicitly test that elements and placeholders are not modified when a translation is missing or the requested language is missing.
- Tests cover `docs/index.html` inline JS functionality for localization.
