# Active sessions

Keep only recent, relevant handoffs. New pages use `SES-YYYYMMDD-HHMMSS-<short-id>` in frontmatter and the session synthesis template.

When active memory exceeds its budget, use `memory-rotation` to curate and move old sessions to `wiki/archive/YYYY/`.
