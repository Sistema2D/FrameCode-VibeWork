# S001

## Context
Fixed a potential XSS vulnerability in translation rendering in `docs/index.html`.
The application was using `innerHTML` to set text from translations file, rendering it vulnerable.
We substituted the assignment with `textContent`.
Bumped versions to `V0.7.6`.
